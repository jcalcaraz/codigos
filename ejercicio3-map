let width=400,height=400;
let angulo=0;
function setup() {
  createCanvas(width, height);
  background(220);
}

function draw() {
  let x = map(cos(angulo),-1,1,0,width);
  let y = map(sin(angulo),-1,1,0,height);
  line(200,200,x,y); 
  angulo += 0.02;
}
